export type Trip = {
    title: string;
    description: string;
    placeCount: number; //nbr de places dans un trip
    userId: string
    _id: string;
    createdAt: Date;
    modifiedAt: Date;
    __v: number;
  };